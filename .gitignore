node_modules/
dist/
.astro/
.DS_Store
npm-debug.log*
.env
.env.*

# Claude Code local files. CLAUDE.md stays on disk so future sessions still
# read it, but it is never committed: it documents the disclosure policy and
# which vendors have open reports, which is not public information.
.claude/
CLAUDE.md
